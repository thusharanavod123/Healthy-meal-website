import { redirect } from "next/navigation"; import { getAdmin } from "@/lib/auth"; import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input"; import { login } from "./actions";
export default async function LoginPage({ searchParams }: { searchParams: Promise<{ error?: string; next?: string }> }) { if (await getAdmin()) redirect("/admin"); const query = await searchParams; return <div className="flex min-h-screen items-center justify-center bg-[#f1f5ed] p-5"><div className="w-full max-w-md rounded-2xl bg-white p-8 shadow-sm"><p className="eyebrow">FreshTable</p><h1 className="display mt-2 text-4xl font-bold">Admin sign in</h1><p className="mt-3 text-sm text-[#63766c]">Approved administrators only.</p>{query.error && <p role="alert" className="mt-5 rounded-lg bg-red-50 p-3 text-sm font-bold text-red-800">{query.error}</p>}<form action={login} className="mt-7 grid gap-5"><input type="hidden" name="next" value={query.next || "/admin"} /><label className="grid gap-2 text-sm font-bold">Email<Input type="email" name="email" autoComplete="email" required /></label><label className="grid gap-2 text-sm font-bold">Password<Input type="password" name="password" autoComplete="current-password" required /></label><Button type="submit">Sign in</Button></form></div></div> }
