import { AdminShell } from "@/components/admin/admin-shell"; import { CategoryManager } from "@/components/admin/category-manager"; import { getAdminCategories } from "@/lib/admin-data"; import { requireAdmin } from "@/lib/auth";
export default async function CategoriesPage({ searchParams }: { searchParams: Promise<{ error?: string }> }) { await requireAdmin(); const [categories, query] = await Promise.all([getAdminCategories(),searchParams]); return <AdminShell><p className="eyebrow">Taxonomy</p><h1 className="mt-2 text-3xl font-bold">Categories</h1>{query.error && <p className="mt-4 rounded-lg bg-red-50 p-3 text-sm font-bold text-red-800">{query.error}</p>}<div className="mt-8"><CategoryManager categories={categories}/></div></AdminShell> }
