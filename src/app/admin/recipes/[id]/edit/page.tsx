import { notFound } from "next/navigation"; import { RecipeForm } from "@/components/admin/recipe-form"; import { AdminShell } from "@/components/admin/admin-shell"; import { getAdminCategories, getAdminRecipe } from "@/lib/admin-data"; import { requireAdmin } from "@/lib/auth";
export default async function EditRecipePage({ params }: { params: Promise<{ id: string }> }) { await requireAdmin(); const { id } = await params; const [recipe,categories] = await Promise.all([getAdminRecipe(id),getAdminCategories()]); if (!recipe) notFound(); return <AdminShell><p className="eyebrow">Recipes</p><h1 className="mt-2 text-3xl font-bold">Edit recipe</h1><div className="mt-8 max-w-4xl"><RecipeForm recipe={recipe} categories={categories}/></div></AdminShell> }
