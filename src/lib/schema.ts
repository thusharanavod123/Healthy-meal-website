import type { Recipe } from "@/types/content";
export function buildRecipeSchema(recipe: Recipe, siteUrl: string) { const total = recipe.prepMinutes + recipe.cookMinutes; return { "@context": "https://schema.org", "@type": "Recipe", name: recipe.title, description: recipe.description, image: [recipe.image.startsWith("http") ? recipe.image : `${siteUrl}${recipe.image}`], author: { "@type": "Person", name: recipe.author }, datePublished: recipe.publishedAt, dateModified: recipe.updatedAt, prepTime: `PT${recipe.prepMinutes}M`, cookTime: `PT${recipe.cookMinutes}M`, totalTime: `PT${total}M`, recipeYield: `${recipe.servings} servings`, recipeCategory: recipe.category.name, keywords: recipe.tags.join(", "), nutrition: { "@type": "NutritionInformation", calories: `${recipe.calories} calories`, proteinContent: `${recipe.protein} g`, carbohydrateContent: `${recipe.carbs} g`, fatContent: `${recipe.fat} g` }, recipeIngredient: recipe.ingredients, recipeInstructions: recipe.instructions.map((text, i) => ({ "@type": "HowToStep", position: i + 1, text })) }; }
