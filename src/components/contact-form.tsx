"use client";
import { useActionState } from "react"; import { submitContact, type ContactState } from "@/app/contact/actions"; import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input"; import { Textarea } from "@/components/ui/textarea";
const initial: ContactState = { ok: false, message: "" };
export function ContactForm() { const [state, action, pending] = useActionState(submitContact, initial); return <form action={action} className="grid gap-5 rounded-3xl bg-[#f3f6f0] p-7 sm:p-10"><div className="hidden" aria-hidden><label>Website<Input name="website" tabIndex={-1} autoComplete="off" /></label></div><label className="grid gap-2 text-sm font-bold">Name<Input name="name" required /></label><label className="grid gap-2 text-sm font-bold">Email<Input type="email" name="email" required /></label><label className="grid gap-2 text-sm font-bold">Message<Textarea name="message" minLength={10} maxLength={5000} required /></label><Button type="submit" disabled={pending}>{pending ? "Sending…" : "Send message"}</Button>{state.message && <p role="status" className={`text-sm font-bold ${state.ok ? "text-green-700" : "text-red-700"}`}>{state.message}</p>}<p className="text-xs text-[#6b7c73]">Validated securely on the server. Delivery integration is pending.</p></form> }
